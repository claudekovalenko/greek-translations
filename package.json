{
  "name": "anagnosis",
  "version": "0.1.0",
  "description": "Greek New Testament reading, translation and parsing workbench",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "npx http-server . -p 8080 -c-1",
    "fetch-data": "node scripts/fetch-data.mjs",
    "build-lexicon": "node scripts/build-lexicon.mjs",
    "make-icons": "node scripts/make-icons.mjs",
    "build": "node scripts/build-single.mjs",
    "build:embed": "node scripts/build-single.mjs --embed"
  },
  "license": "MIT"
}
